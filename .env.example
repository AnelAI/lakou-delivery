# Database (Neon PostgreSQL)
# Get these from https://console.neon.tech — create a project, then copy the connection strings
DATABASE_URL="postgresql://user:password@ep-xxx.region.aws.neon.tech/neondb?sslmode=require&pgbouncer=true"
DIRECT_URL="postgresql://user:password@ep-xxx.region.aws.neon.tech/neondb?sslmode=require"

# Pusher Channels (real-time)
# Get these from https://dashboard.pusher.com — create a Channels app
NEXT_PUBLIC_PUSHER_KEY=""
NEXT_PUBLIC_PUSHER_CLUSTER="eu"
PUSHER_APP_ID=""
PUSHER_SECRET=""
